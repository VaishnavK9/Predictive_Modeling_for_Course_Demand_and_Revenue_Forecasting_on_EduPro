# Runtime + full-pipeline dependencies for the EduPro forecasting project.
# scikit-learn is pinned EXACTLY so the pickled models in models/ load without
# version-mismatch errors. Other packages use compatible-release ranges so the
# app installs cleanly on Streamlit Cloud across Python 3.11-3.13.
scikit-learn==1.6.1
numpy>=1.26,<2.3
pandas>=2.1,<2.3
matplotlib>=3.8,<3.11
seaborn>=0.13,<0.14
statsmodels>=0.14,<0.15
streamlit>=1.37,<1.42
joblib>=1.3,<1.5
openpyxl>=3.1,<3.2
