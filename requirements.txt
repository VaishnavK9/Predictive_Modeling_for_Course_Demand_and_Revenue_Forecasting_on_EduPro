# Exact, mutually ABI-compatible versions (verified running locally on
# Python 3.13). The scientific core (numpy/scipy/scikit-learn/pandas) MUST be
# pinned together: mixing a numpy 1.x with wheels compiled against numpy 2.x
# causes a native segmentation fault at import on Streamlit Cloud.
#
# These versions require Python >= 3.10 — set the app's Python version to 3.13
# in Streamlit Cloud (Manage app -> Settings -> Advanced) to match.
numpy==2.2.1
scipy==1.18.0
scikit-learn==1.6.1
threadpoolctl==3.6.0
pandas==2.2.3
joblib==1.4.2
matplotlib==3.10.0
seaborn==0.13.2
statsmodels==0.14.4
streamlit==1.41.1
openpyxl==3.1.5
